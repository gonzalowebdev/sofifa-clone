<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/panel.css">
    <title>Admin Panel</title>
</head>
<body>
    <div class="nav-panel">
        <div class="brand" style="display: flex; align-items: center;">
            <h1 style=" margin-right: 30px;"><a href="/panel">SOFIFA</a></h1>
            <div>
                <h5><a href="/">Home</a></h5>
            </div>
        </div>
        <div class="user-data"><h4>user</h4></div>
    </div>
    <div class="main-panel">
        <div class="aside-panel">
            <div class="title"></div>
            <div class="card-panel-aside">
                <a href="/panel/players">Players</a>
            </div>
            <div class="card-panel-aside">
                <a href="/panel/teams">Teams</a>
            </div>
            <div class="card-panel-aside">
                <a href="/panel/nateams">National Teams</a>
            </div>
        </div>
        <div class="content-panel">
            <div style="display: flex;">
                <a href="/newnateam" class="btn btn-bordered" style="margin-right: 12px;">New nat. team</a>
                <a href="/newteam" class="btn btn-bordered">New team</a>
            </div>
            <div style="margin-top: 30px;">
                <h3>ALL NATIONAL TEAMS</h3>
                <div style="margin-top: 30px;">
                    <% teams.forEach(team =>{%>
                    <div class="card-panel-item">
                        <div class="data-item">
                            <% if (team.flag){%>
                                <img src="/img/<%= team.flag %>" height="40%" style="margin-right:30px;">
                            <%}%>
                            <% if (team.shield){%>
                                <img src="/img/<%= team.shield %>" height="40%" style="margin-right:30px;">
                            <%}%>
                            <div><%= team.name %></div>
                            <div><%= team.rating %></div>
                        </div>
                        <div class="accion-item" style="display: flex; align-items: center;">
                            <button class="btn-accion">
                                <a href="/edit/<%= team._id%>">Edit</a>
                            </button>
                            <div>
                                <form action="/delete/<%= team._id%>?_method=DELETE" method="POST">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button type="submit" class="btn-accion">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                    <%})%>
                </div>
            </div>
        </div>
    </div>
</body>
</html>